# Output the entire file to the console
cat file.txt

# Output the file to the console by pages (space - next page, q - quit)
less file.txt

# Creates a file if not existing or updates file modification date-time
touch Test-1.txt










